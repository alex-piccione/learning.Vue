import CookieService from "./Cookies.service"

type User = {
    Name:string
}

let readCookie = (cookie:string) =>
{
    CookieService
}

export default class AuthService {

    login = (username:string, passwotrd:string) => {
        //CookieService
    }

}